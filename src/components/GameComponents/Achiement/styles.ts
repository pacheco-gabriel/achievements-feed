import { StyleSheet } from 'react-native';
import { COLORS } from '../../../theme';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    height: 120,
    flexDirection: 'column',
    color: '#383838'
  },
  achiementImg: {

  },
  achiementTitle: {
    fontSize: 14,
    color: COLORS.WHITE,
  },
  achiementDescription: {
    fontSize: 12,
    color: COLORS.WHITE,
  }
});
